[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Use the Docker CMD which runs entrypoint.sh (backend + celery worker + beat)
healthcheckPath = "/docs"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
